- args:
    sql: CREATE TABLE public."project"("id" serial NOT NULL UNIQUE, "user_id" integer
      NOT NULL, "title" text NOT NULL, "description" text , "status" text NOT NULL,
      PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: project
    schema: public
  type: add_existing_table_or_view
